<br />
<div class="booyah-box col-xs-10 col-xs-offset-1">
  <%= @question.category %>
  <%=  @question.question %>
  <%= simple_form_for @user_answer do |f| %>
    <%= a.input :user_answer, collection: [@answers], as: :check_boxes %>
  <br />
  <%= f.submit "Create", :class => 'btn btn-primary' %>
  <br />
  <% end %>
<br />
</div>